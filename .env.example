# Example environment variables for scraps
# Copy this file to .env and fill in the values.

DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/your_webhook_id/your_webhook_token
SESSION_COOKIE=your_session_cookie_here

# Notes:
# - Set SESSION_COOKIE to the raw session token (do not include "session=").
# - The app constructs a cookie header using the session value for api.scraps.hackclub.com.
